version: '3'
tasks:
  default:
    desc: 'Listing commands'
    cmds:
      - task -l
  tidy:
    desc: 'Tidy project'
    cmds:
      - go generate ./...
      - go mod tidy
  buf:gen:
    desc: 'Generate buf'
    cmds:
      - buf generate --path=./proto
